# Use a deterministic audit engine with an optional AI reviewer

The CI outcome must be reproducible and evidence-grounded, so Journey execution,
keyboard actions, Finding classification, and Baseline comparison are
deterministic. AI runs only after a Finding exists and may explain permitted
evidence without changing audit outcomes; autonomous exploration remains a
separate future mode.

