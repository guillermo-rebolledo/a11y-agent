{
  "name": "a11y-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <27"
  },
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "build": "pnpm build:extension && pnpm -r --if-present build",
    "dev": "pnpm --parallel --filter @a11y-agent/web --filter @a11y-agent/api dev",
    "format": "prettier --check package.json pnpm-workspace.yaml .env.example vercel.json apps/api apps/web packages/domain packages/ui docs/customer-github-configuration.md",
    "format:write": "prettier --write package.json pnpm-workspace.yaml .env.example vercel.json apps/api apps/web packages/domain packages/ui docs/customer-github-configuration.md",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm --filter @a11y-agent/domain build && pnpm build:extension && vitest run",
    "test:a11y": "pnpm --filter @a11y-agent/web test:a11y",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc -p apps/extension/tsconfig.json --noEmit && pnpm -r --if-present typecheck",
    "check": "pnpm format && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:a11y",
    "build:extension": "node apps/extension/scripts/clean.mjs && tsc -p apps/extension/tsconfig.json && node apps/extension/scripts/assemble.mjs",
    "package:extension": "pnpm build:extension && node apps/extension/scripts/package.mjs",
    "proof:mem-7": "tsx src/mem-7/cli.ts run",
    "proof:mem-7:verify": "tsx src/mem-7/cli.ts verify",
    "proof:mem-9:verify": "vitest run src/mem-22/evidence-bundle.test.ts src/mem-22/publication.test.ts src/mem-22/action-contract.test.ts src/mem-9",
    "proof:mem-10:verify": "vitest run src/mem-10/qualification.test.ts && shasum -a 256 -c evidence/MEM-10/CHECKSUMS.sha256"
  },
  "dependencies": {
    "@vercel/sandbox": "2.9.0",
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@types/async-retry": "1.4.9",
    "@types/chrome": "0.1.32",
    "@types/node": "24.10.1",
    "jose": "6.1.0",
    "next": "16.2.12",
    "oxlint": "1.41.0",
    "playwright": "1.61.1",
    "prettier": "3.9.6",
    "tsx": "4.20.6",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "yaml": "2.8.1"
  }
}
